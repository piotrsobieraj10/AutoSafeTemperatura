kind = "web"
previewPath = "/"
title = "AutoSafe Temperatura"
version = "1.0.0"
id = "artifacts/autosafe"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20512

[services.development]
run = "pnpm --filter @workspace/autosafe run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/autosafe", "run", "build" ]
publicDir = "artifacts/autosafe/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20512"
BASE_PATH = "/"
